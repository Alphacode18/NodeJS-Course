const hobbies = ['Sports', 'Cooking'];
hobbies.forEach(element => console.log(element));
const iLoveHobbies = hobbies.map(element => `I love ${element}`);
iLoveHobbies.forEach(element => console.log(element));
//Arrays are heterogenous and can use iterative functions which are basically higher order functions