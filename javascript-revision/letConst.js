let name = 'Shreyas';
let age = 19;
const hasHobbies = true; //Use const as much more as possible.

function userSummary(name, age) {
    return `Nice to meet you ${name}. I am also ${age} years old`;
}

console.log(userSummary(name, age));